# Pinned — a hackathon demo must not break because a transitive release changed.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
numpy==2.2.1
scipy==1.15.0

# Dev only. Not imported by app/ at runtime.
pytest==8.3.4
httpx==0.28.1
