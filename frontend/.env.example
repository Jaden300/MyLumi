# Base URL of the MyLumi inference API (backend/). Leave unset to run the app
# with no network calls at all - the insights section simply does not render.
#
# Local:  http://localhost:8000
# Render: https://mylumi-api.onrender.com
VITE_API_URL=
